# Optional: absolute API origin (no trailing slash; `/api` is appended in code).
# Leave unset for `npm run dev` — requests use `/api` and Vite proxies to the backend on :3000.
# PUBLIC_API_BASE_URL=http://127.0.0.1:3000
