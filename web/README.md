# Web (SvelteKit)

Frontend for the Analyzer project. Setup, environment variables, and run instructions are in the [root README](../README.md).

## Commands

```bash
npm run dev      # http://localhost:5173 (proxies /api → :3000)
npm run build    # production build
npm run preview  # serve build (default :4173)
```
